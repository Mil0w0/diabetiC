#include <stdlib.h>
#include <stdio.h>
#include <string.h>

// Une fonction pour :
// changer username
// changer password
// changer glycemia aim (à voir avec la personne qui le fait)

void updateTable(char *sql);

void updatePassword(char *username);

void updateUsername(char *username);

void deleteProfile(char *username);